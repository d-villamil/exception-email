{
  "name": "late-shipment-outreach",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Daily automation that drafts carrier outreach for late shipments without exceptions.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --import tsx tests/**/*.test.ts",
    "authorize": "tsx src/gmail/authorize.ts",
    "run:dryrun": "tsx src/jobs/daily-outreach.ts --source=fixture --mode=draft"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "exceljs": "^4.4.0",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
